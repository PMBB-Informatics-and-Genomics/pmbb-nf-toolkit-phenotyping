/*
 * SLURM executor profile for HPC clusters
 */

process {
    executor       = 'slurm'
    clusterOptions = '--account=default'

    withName: 'ADVANCED_PROFILING' {
        cpus   = 4
        memory = '32.GB'
        time   = '4.h'
    }
}

executor {
    queueSize = 50
}
